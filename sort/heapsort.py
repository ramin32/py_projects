def left(x):
    return 2 * x

def right(x):
    return 2 * x + 1

def parent(x)
    return x / 2

def is_leaf(list, index):
    return index > (len(list) / 2)

def heapify(list, index):
    if is_leaf(list, index):
        return
    if left(index)

def build_max_heap(list)
